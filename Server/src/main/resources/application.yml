
server:
  port: 8070

spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/project_yuri"
    username: "root"
    password: "password"

  jpa:
    database: MYSQL
    open-in-view: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
#        show_sql: true
#        format_sql: true
